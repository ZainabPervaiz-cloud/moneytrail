# Copy to ".env" for local overrides (git-ignored). Points the frontend
# at the backend API — change this to your deployed backend URL in
# production hosting (e.g. Vercel/Netlify environment variables).
VITE_API_BASE_URL=http://localhost:8000/api
